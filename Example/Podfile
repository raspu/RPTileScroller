source 'https://github.com/CocoaPods/Specs.git'

target 'RPTileScroller', :exclusive => true do
  pod "RPTileScroller", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "RPTileScroller", :path => "../"

  pod 'Specta', '~> 0.2.1'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
